<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:MyApp.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="MyApp.Views.BudgetsView"
             x:DataType="vm:BudgetsViewModel">
  <ScrollViewer>
    <StackPanel Spacing="10" Margin="20">
      <TextBlock Text="Budgets Overview" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
      
      <!-- Placeholder for budget categories and progress -->
      <Border Classes="ContentArea" CornerRadius="5" Padding="15" MinHeight="300">
        <TextBlock Text="Budget categories, spending progress, and management tools will be displayed here."
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"/>
      </Border>

      <!-- Placeholder for actions -->
      <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,20,0,0">
        <Button Content="Create New Budget"/>
        <Button Content="Manage Categories"/>
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl> 